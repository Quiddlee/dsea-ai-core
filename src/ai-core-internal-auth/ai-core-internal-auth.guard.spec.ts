import { AiCoreInternalAuthGuard } from './ai-core-internal-auth.guard';

describe('AiCoreInternalAuthGuard', () => {
  it('should be defined', () => {
    expect(new AiCoreInternalAuthGuard()).toBeDefined();
  });
});
